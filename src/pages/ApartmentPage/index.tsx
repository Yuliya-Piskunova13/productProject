import React from 'react';
import {SafeAreaView} from 'react-native-safe-area-context';

import {Text} from '../../shared/ui/Text';

const ApartmentPage = () => {
  return (
    <SafeAreaView style={{flex: 1}}>
      <Text>ApartmentPage</Text>
    </SafeAreaView>
  );
};

export default ApartmentPage;
